server:
  port: 8200

spring:
  application:
    name: monitor-be
  datasource:
    url: jdbc:mysql://120.53.92.66:3306/youbi?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: hoshuuch
    password: 490229
    driver-class-name: com.mysql.cj.jdbc.Driver

management:
  endpoints:
    web:
      exposure:
        include: health

youbi:
  minio:
    endpoint: ${YDBI_MINIO_ENDPOINT:http://120.53.92.66:9000}
    access-key: ${YDBI_MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${YDBI_MINIO_SECRET_KEY:minioadmin}
    bucket: ${YDBI_MINIO_BUCKET:ydbi}
    work-dir: ${YDBI_MONITOR_UPLOAD_WORK_DIR:${java.io.tmpdir}/ydbi/monitor-be/uploads}
  alidrive:
    account-key: ${YDBI_ALIDRIVE_ACCOUNT_KEY:default}
    refresh-token: ${YDBI_ALIDRIVE_REFRESH_TOKEN:}
    work-dir: ${YDBI_ALIDRIVE_WORK_DIR:${java.io.tmpdir}/ydbi/monitor-be/alidrive}
  xiaohongshu:
    headless: ${YDBI_XHS_HEADLESS:true}
    browser-channel: ${YDBI_XHS_BROWSER_CHANNEL:chrome}
  douyin:
    headless: ${YDBI_DOUYIN_HEADLESS:true}
    browser-channel: ${YDBI_DOUYIN_BROWSER_CHANNEL:chrome}
    cdp-url: ${YDBI_DOUYIN_CDP_URL:}
    cdp-endpoints: ${YDBI_DOUYIN_CDP_ENDPOINTS:}
  bilibili:
    playwright:
      headless: ${YDBI_BILIBILI_PLAYWRIGHT_HEADLESS:false}
      browser-channel: ${YDBI_BILIBILI_PLAYWRIGHT_BROWSER_CHANNEL:}
